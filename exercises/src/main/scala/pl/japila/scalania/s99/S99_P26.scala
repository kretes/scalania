package pl.japila.scalania.s99

object S99_P26 {
  def combinations[T](count: Int, ts: Seq[T]): Seq[Seq[T]] = {
    combinations(count,ts,List());
  }

  def combinations[T](count: Int, ts: Seq[T], current:Seq[T]): Seq[Seq[T]] = {
    ???
  }
}
